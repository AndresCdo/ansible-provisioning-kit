---
# tasks file for essentials role
- name: Install essential packages
  ansible.builtin.apt:
    name:
      - build-essential
      - vim
      - curl
      # Removed zsh - handled by zsh role
    state: present
    update_cache: yes
