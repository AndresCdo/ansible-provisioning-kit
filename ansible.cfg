[defaults]
inventory = inventory/hosts.yml
roles_path = roles
host_key_checking = False
# Uncomment if you have vault-encrypted files
# vault_password_file = ~/.ansible_vault_pass

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False